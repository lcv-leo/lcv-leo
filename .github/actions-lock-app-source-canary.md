# Dedicated App status canary

Temporary, non-production marker used to verify that the `Validate actions.lock`
commit status is published by the dedicated GitHub App before the repository
ruleset is bound to that App as its expected source.
