# Único ecossistema deste repositório: GitHub Actions (snake.yml, codeql.yml).
# As actions são pinadas por SHA; o Dependabot atualiza o pin e o comentário de versão.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
