# Actions lock positive canary

Temporary, non-production artifact used to prove that the trusted validator
accepts an unchanged, semantically complete `actions.lock` on a pull request
opened after the validator reached the default branch.
