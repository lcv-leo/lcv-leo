# This workflow is managed by gh actions-lock.

name: Scorecard supply-chain security

on:
  branch_protection_rule:
  schedule:
    - cron: "37 8 * * *"
  push:
    branches:
      - main

permissions: {}

concurrency:
  group: scorecard-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analysis:
    name: Scorecard analysis
    if: ${{ github.event.repository.default_branch == github.ref_name || github.event_name == 'branch_protection_rule' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read # Read workflow metadata used by Scorecard checks.
      checks: read # Discover configured security checks.
      contents: read # Analyze repository history and configuration.
      issues: read # Read security-policy issue metadata.
      pull-requests: read # Inspect repository contribution practices.
      security-events: write # Upload SARIF to code scanning.
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run OpenSSF Scorecard
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: false

      - name: Upload Scorecard artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

      - name: Upload Scorecard results to code scanning
        uses: github/codeql-action/upload-sarif@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          sarif_file: results.sarif
